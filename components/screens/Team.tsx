import TeamCards from "@/components/molecules/TeamCards";

export default function Team() {
  return (
    <section id="team" aria-label="Meet the Team" className="scroll-mt-20">
      <TeamCards />
    </section>
  );
}
